//Crea una variable llamada `radio` y asígnale el radio de un círculo. Calcula el área del
//círculo utilizando la fórmula `A = π * r^2` y muestra el resultado en la consola.

let radio = parseInt(prompt("Ingrese el radio de un círculo"));

let a = 2 * Math.PI * radio; 

alert(`El área del círculo es ${a}`);

